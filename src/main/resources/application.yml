spring:
  datasource:
    url: jdbc:mysql://localhost:3306/queueless
    username: root
    password: root987
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
server:
  port: 8080
